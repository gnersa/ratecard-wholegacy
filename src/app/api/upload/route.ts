import { NextResponse } from "next/server";
import { put } from "@vercel/blob";
import { getCurrentUser } from "@/lib/auth";
export const runtime = "nodejs";
const allowedKinds=new Set(["avatar","cover"]),allowedTypes=new Set(["image/jpeg","image/png","image/webp"]),MAX=5*1024*1024;
export async function POST(request:Request){const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});if(!process.env.BLOB_READ_WRITE_TOKEN)return NextResponse.json({error:"BLOB_READ_WRITE_TOKEN belum tersedia. Hubungkan Vercel Blob ke project lalu redeploy."},{status:500});try{const form=await request.formData();const file=form.get("file"),kind=String(form.get("kind")||"");if(!(file instanceof File))return NextResponse.json({error:"File gambar tidak ditemukan."},{status:400});if(!allowedKinds.has(kind))return NextResponse.json({error:"Jenis upload tidak valid."},{status:400});if(!allowedTypes.has(file.type))return NextResponse.json({error:"Gunakan JPG, PNG, atau WebP."},{status:400});if(file.size>MAX)return NextResponse.json({error:"Ukuran maksimal 5 MB."},{status:400});const ext=(file.name.split(".").pop()||"jpg").toLowerCase();const blob=await put(`ratecard/${user.id}/${kind}-${Date.now()}.${ext}`,file,{access:"public",token:process.env.BLOB_READ_WRITE_TOKEN,addRandomSuffix:true});return NextResponse.json({ok:true,url:blob.url})}catch(e){console.error("blob upload",e);return NextResponse.json({error:"Upload ke Vercel Blob gagal. Periksa Blob Store dan BLOB_READ_WRITE_TOKEN."},{status:500})}}
